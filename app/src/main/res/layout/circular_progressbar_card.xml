<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="160dp"
    android:layout_height="170dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:elevation="20dp"
    app:cardCornerRadius="12dp"
    android:background="@color/gray"
    android:backgroundTint="@color/gray"
    android:layout_margin="10dp"
    android:id="@+id/circular_progressbar_card">
    <TextView
        android:id="@+id/progress_card_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/card_text"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/white"
        android:textAlignment="center"
        android:layout_marginVertical="8dp"/>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/circular_progressbar_indicator"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="10dp"
        app:indicatorSize="120dp"
        android:layout_gravity="center"
        app:trackThickness="10dp"
        app:trackCornerRadius="15dp"
        android:progress="40"
        app:trackColor="@color/dark_gray"
        app:indicatorColor="@color/green" />

        <TextView
            android:id="@+id/progress_card_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="24sp"
            android:layout_gravity="center"
            android:textStyle="bold"
            android:textColor="@color/green"
            android:layout_marginTop="-8dp"
            android:text="Kcal" />

        <TextView
            android:id="@+id/progress_card_percentage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:layout_gravity="center"
            android:layout_marginTop="23dp"
            android:textColor="@color/white"
            android:text="10%" />
    </FrameLayout>

</com.google.android.material.card.MaterialCardView>